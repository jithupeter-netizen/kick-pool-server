{
  "name": "kick-pool-server",
  "version": "2.1.0",
  "description": "WebSocket server with authoritative server-side physics for Kick Pool (zero external deps)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
